import { observer } from "mobx-react-lite";
import { Header } from "semantic-ui-react";

const PlantRecordsComponent = observer(function PlantRecords() {

    return (
        <Header>
        zaznamy
        </Header>
    )
})

export default PlantRecordsComponent;